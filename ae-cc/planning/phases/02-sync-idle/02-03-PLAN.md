---
phase: 02-sync-idle
plan: 03
type: execute
---

<objective>
Implement the event system for logging, webhooks, and callbacks.

Purpose: Events are how MailPilot communicates state changes to agents. Every sync, send, delete, and tag operation emits an event. Agents poll events or receive webhooks to stay aware of inbox changes.
Output: Event emitter that logs to SQLite, optionally fires webhooks, and supports Python callbacks. All event types from the PRD implemented.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@./ae-cc/planning/BRIEF.md
@./ae-cc/planning/ROADMAP.md
@./ae-cc/planning/phases/02-sync-idle/02-02-SUMMARY.md
@src/mailpilot/database.py
@src/mailpilot/models.py
@src/mailpilot/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define event types and implement event emitter</name>
  <files>src/mailpilot/events/__init__.py, src/mailpilot/events/types.py, src/mailpilot/events/emitter.py</files>
  <action>
  Event types (`events/types.py`):
  - Define `EventType` as a string enum: `new_email`, `email_read`, `email_sent`, `email_deleted`, `email_moved`, `email_tagged`, `sync_started`, `sync_completed`, `error`, `connection_lost`, `connection_restored`
  - Each event type has a description docstring

  Event emitter (`events/emitter.py`):
  Create `EventEmitter` class:

  `__init__(self, db: Database, webhook_urls: dict[str, str | None] | None = None)`
  - `db` for SQLite logging
  - `webhook_urls` maps account_name → webhook URL (from config)
  - `_callbacks: list[Callable]` — registered Python callbacks

  `async def emit(self, event_type: EventType, account_id: int | None = None, message_id: int | None = None, details: dict | None = None)`:
  1. Insert event into SQLite events table via `db.insert_event()`
  2. If webhook_url configured for this account: fire async HTTP POST
     - POST body: JSON with event_type, account_id, message_id, details, timestamp
     - Use `aiohttp.ClientSession` (add aiohttp to deps) or `urllib.request` in a thread
     - Fire-and-forget: don't block on webhook response, log errors
     - Timeout: 10 seconds
  3. For each registered callback: call `callback(event)` — wrap in try/except so one bad callback doesn't break others

  `def register_callback(self, callback: Callable)` — add to _callbacks list
  `def unregister_callback(self, callback: Callable)` — remove from _callbacks

  `async def get_events(self, event_type: str | None = None, since: str | None = None, limit: int = 50) -> list[Event]`:
  - Parse `since` string: "1h", "30m", "2d", "2026-03-28" — convert to datetime
  - Query db.get_events with filters
  - Return list of Event model objects

  `events/__init__.py`:
  - Export EventType, EventEmitter

  Note: For the webhook HTTP POST, use a lightweight approach. If aiohttp feels too heavy, use `asyncio.to_thread` with `urllib.request.urlopen`. Don't add a heavy dependency just for webhooks. Actually, check if `aiohttp` is already needed elsewhere — if not, use `asyncio.to_thread(urllib.request.urlopen, ...)` to keep deps minimal.
  </action>
  <verify>pytest tests/test_events.py -v passes</verify>
  <done>EventEmitter logs to SQLite, fires webhooks, calls Python callbacks. All 11 event types defined. Time-relative parsing works ("1h", "30m", "2d").</done>
</task>

<task type="auto">
  <name>Task 2: Wire events into sync and IDLE</name>
  <files>src/mailpilot/imap/sync.py (modify), src/mailpilot/imap/idle.py (modify), src/mailpilot/daemon.py (modify)</files>
  <action>
  Add EventEmitter integration to existing modules:

  SyncEngine modifications:
  - Accept `event_emitter: EventEmitter` in constructor
  - In `full_sync`: emit `sync_started` at beginning, `sync_completed` at end (with details: message count, duration)
  - In `full_sync`/`incremental_sync`: emit `new_email` for each new message synced (with mp_id in details)
  - On sync errors: emit `error` event with error details

  IdleListener modifications:
  - Accept `event_emitter: EventEmitter` in constructor
  - On new mail detected: emit `new_email` events (delegated to sync which already emits them)
  - On connection lost: emit `connection_lost` event
  - On reconnect success: emit `connection_restored` event

  Daemon modifications:
  - Create EventEmitter during `start()`, pass to SyncEngine and IdleListener constructors
  - Wire webhook_urls from account configs

  Keep changes minimal — just add the emitter parameter and emit() calls at the right points. Don't refactor existing logic.
  </action>
  <verify>Existing sync/idle/daemon tests still pass. New event emission verified in test_events.py.</verify>
  <done>Events emitted at all key points: sync start/complete, new email, connection lost/restored, errors. Existing tests unbroken.</done>
</task>

<task type="auto">
  <name>Task 3: Write event system tests</name>
  <files>tests/test_events.py</files>
  <action>
  Tests:
  - `test_emit_stores_in_db` — emit event, query db, verify stored
  - `test_emit_calls_callbacks` — register callback, emit, verify called with correct event
  - `test_callback_error_doesnt_break_others` — register 2 callbacks (first raises), verify second still called
  - `test_get_events_by_type` — emit multiple types, filter by type, verify correct subset
  - `test_get_events_since_relative` — emit events with different timestamps, query since="1h", verify time filter
  - `test_get_events_limit` — emit 10 events, query with limit=5, verify 5 returned
  - `test_webhook_fire_and_forget` — mock urllib, emit with webhook configured, verify POST fired
  - `test_parse_since_formats` — test "1h", "30m", "2d", "2026-03-28" all parse correctly

  Use in-memory SQLite and AsyncMock for webhook testing.
  </action>
  <verify>pytest tests/test_events.py -v — all pass</verify>
  <done>8+ tests covering event storage, callbacks, webhooks, filtering, and time parsing. All pass. Phase 2 complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` — ALL tests pass (including previously written ones)
- [ ] `ruff check src/mailpilot/events/` — no lint errors
- [ ] Events are wired into sync, IDLE, and daemon
</verification>

<success_criteria>
- All 11 event types defined and emittable
- Events stored in SQLite with correct metadata
- Webhook POST fires asynchronously on events
- Python callbacks supported with error isolation
- Events wired into sync (start/complete/new), IDLE (connection), and daemon
- Time-relative filtering works
- All tests pass
- Phase 2 Sync + IDLE is complete
</success_criteria>

<output>
After completion, create `./ae-cc/planning/phases/02-sync-idle/02-03-SUMMARY.md`
</output>
